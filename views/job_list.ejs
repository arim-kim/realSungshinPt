<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="./css/job-list.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/reset-css@5.0.1/reset.min.css" />
    <title>성신한 알바</title>
</head>
<body>
    <% for(var i = 0; i < data.length; i++) {%>
        <div class = "scdlBox">
            <button class ="btns" style = "border-bottom: 3px solid <%= data[i].parttime.color%>; 
                border-top: 3px solid <%= data[i].parttime.color%>;" id = 'btn'+'<%=data[i].idSchedule%>'>
                <font color = " <%= data[i].parttime.color%>"><%=data[i].parttime.parttimeName%></font>
                <br> 시작시간 :  <%=data[i].startTime%> 
                <br> 끝나는 시간 : <%=data[i].endTime%> 
                <% if (data[i].isCovered == 0) { %>
                    <br> 대타 : X 
                <% } %>
                <% if (data[i].isCovered == 1) { %> 
                <br> 대타 : O 
            <% } %>
            </button>
            <form class = "job-list" action="/job-list" method="post">
                <input type = "hidden" name = "idSchedule" value= "<%=data[i].idSchedule%>">
                <input type = "submit" id = "scdlDelete" value = "delete" >
            </form>
        </div>
            <% if (i == data.length -1)  { %>
                <div id = "totalPay"> 
                    오늘의 일급 : <%=dailyPay.total%> 원</div>
                <% } %>
    <% } %>
</body>
</html>